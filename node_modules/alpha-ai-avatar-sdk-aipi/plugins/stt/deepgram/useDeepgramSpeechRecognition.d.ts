import { LiveSchema } from '@deepgram/sdk';
export type UseSpeechRecognitionOptions = {
    apiKey: string;
    onSpeechRecognized: (transcript: string) => void;
    deepgramOptions?: LiveSchema;
};
export declare function useDeepgramSpeechRecognition({ apiKey, onSpeechRecognized, deepgramOptions, }: UseSpeechRecognitionOptions): {
    isRecognizing: boolean;
    startRecognizing: () => Promise<void>;
    stopRecognizing: () => Promise<void>;
};
