import { BaseAvatarController } from './BaseAvatarController';
import { AvatarClientConfig, SynthesizerOptions } from 'alpha-ai-avatar-sdk-js';
export declare class ConversationalAvatarController extends BaseAvatarController {
    constructor(config: AvatarClientConfig);
    updateSystemMessage(message: string): void;
    updateConversationHistory(message: string): void;
    setMicrophoneMute(isMuted: boolean): void;
    connect(videoElement: HTMLVideoElement, audioElement: HTMLAudioElement): Promise<any>;
    speak(text: string, overrideVoice?: SynthesizerOptions): void;
    sendTextResponse(text: string): void;
}
