import { AvatarClient, AvatarClientConfig } from 'alpha-ai-avatar-sdk-js';
export declare class BaseAvatarController {
    avatarClient: AvatarClient;
    constructor(config: AvatarClientConfig);
    stopSpeaking(): void;
    connect(videoElement: HTMLVideoElement, audioElement: HTMLAudioElement): Promise<void>;
    waitUserGestureToPlayAudio(audioElement: HTMLAudioElement): void;
    getAvatars(): Promise<import("alpha-ai-avatar-sdk-js").Avatars>;
    switchAvatar(avatarId: number): void;
    setConversationHistory(messages: {
        role: string;
        content: string;
    }[]): void;
    disconnect(): void;
    getAvatarPrompt(avatarId: number): string | undefined;
}
